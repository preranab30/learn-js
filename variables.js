var a= 20;
let b = 23.0;
const c = 34; // we can not change the value -- block and function scope

   a = 30; // we can re-Initialize the value --- global scope

   b = 'prerana' // we can re-Intialize and also we can change the datatype -- block and function scope
 
console.log(a);
console.log(b);
console.log(c);
console.log(typeof(a)); // for which data type is using
